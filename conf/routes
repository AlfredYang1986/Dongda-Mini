# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
POST    /provider/apply/push        controllers.ProviderController.pushApplies
POST    /provider/apply/pop         controllers.ProviderController.popApplies
POST    /provider/apply/query       controllers.ProviderController.queryApplies
POST    /provider/apply/search      controllers.ProviderController.searchApplies

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
